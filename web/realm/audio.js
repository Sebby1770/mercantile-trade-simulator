// Gentle procedural ambience; no streamed audio, microphone, or external assets.
export function createAudio(settings){let ctx=null,gain=null,lastStep=0,lastBird=0;function ensure(){if(!ctx){ctx=new AudioContext();gain=ctx.createGain();gain.gain.value=settings.sound?.25:0;gain.connect(ctx.destination);}if(ctx.state==='suspended')ctx.resume().catch(()=>{});}
 function tone(freq,duration,volume,type='sine',delay=0){if(!ctx||!settings.sound)return;const o=ctx.createOscillator(),v=ctx.createGain(),t=ctx.currentTime+delay;o.type=type;o.frequency.setValueAtTime(freq,t);v.gain.setValueAtTime(.0001,t);v.gain.exponentialRampToValueAtTime(Math.max(.0002,volume),t+.025);v.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(v);v.connect(gain);o.start(t);o.stop(t+duration+.01);}
 return{start(){try{ensure();}catch{}},set(){if(gain)gain.gain.value=settings.sound?.25:0;},chime(){tone(660,.2,.1);tone(880,.35,.08,'sine',.13);},update(time,moving,sprint,daylight){if(!ctx||!settings.sound)return;if(moving&&time-lastStep>(sprint?.27:.4)){lastStep=time;tone(70+Math.random()*25,.08,.16,'triangle');}if(daylight>.4&&time-lastBird>7+Math.random()*4){lastBird=time;tone(1900,.1,.035);tone(2300,.09,.025,'sine',.13);tone(2100,.12,.03,'sine',.24);}}};}
